# Copy to `.env` and fill in. `.env` is gitignored — never commit real values.

# Google Stitch (AI UI generation).
# Used by: the Stitch MCP server (see .mcp.json) and @google/stitch-sdk.
# Get a key from https://stitch.withgoogle.com -> API access.
STITCH_API_KEY=
NTFY_TOPIC=derma-serp-1787326503-change-me
